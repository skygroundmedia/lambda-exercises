{
  "name": "transcode-wav-to-flac",
  "version": "1.0.0",
  "description": "Transcode WAV file to FLAC format.",
  "main": "index.js",
  "scripts": {
	  "test": "run-local-lambda --file index.js --event tests/event.json",
	  "deploy": "aws lambda update-function-code --function-name arn:aws:lambda:us-east-1:038221756127:function:transcode-video --zip-file fileb://Lambda-Deployment.zip",
	  "predeploy": "zip -r Lambda-Deployment.zip * -x *.zip *.json *.log"
  },
  "author": "Chris Mendez",
  "license": "MIT",
  "devDependencies": {
    "dotenv": "^4.0.0",
    "run-local-lambda": "^1.1.0"
  },
  "dependencies": {
    "aws-sdk": "^2.94.0"
  }
}
